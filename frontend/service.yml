apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: devops-training
spec:
  ports:
  - port: 8080
    targetPort: 80
    protocol: TCP
  selector:
    app: frontend
  type: LoadBalancer
